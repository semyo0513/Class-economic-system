# 클래스뱅크 LMS → 2D 타운 전환 프로젝트 기획서 (plan.md)

> 본 문서는 기존 **클래스뱅크 LMS v16**(탭 기반 GAS 웹앱)을 **동물의 숲 스타일 2D 탐험형 게임 웹앱**으로 전환하기 위한 개발 기획서입니다. 이 문서를 Google Antigravity에 입력하여 실제 코드를 생성하는 것을 전제로, "무엇을 유지하고 무엇을 새로 만들어야 하는지"를 명확한 지시 단위로 정리했습니다.

---

## 0. 이 문서를 읽는 방법

- **🔒 고정 요구사항**: 반드시 지켜야 하는 제약 (기존 DB/로직 보존 등)
- **🎨 설계 제안**: 참조 이미지·요청사항을 바탕으로 한 구체적 설계안 (Antigravity가 그대로 구현하거나, 필요시 조정 가능)
- **⚠️ 기술적 리스크**: 반드시 사전에 인지하고 결정해야 하는 트레이드오프

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 기존 시스템 | 클래스뱅크 LMS v16 — Apps Script(HtmlService) 단독 구동, 탭 기반 UI, `dispatch(action, payload)` 패턴, 구글시트 10개 DB |
| 전환 목표 | 2D 탑다운 마을 탐험형 게임 UI — 기존 탭(마트/은행/부동산 등)을 맵 위의 **건물**로 재해석 |
| 프론트엔드 배포처 | GitHub 저장소 → **GitHub Pages** (정적 호스팅) |
| 백엔드/DB | 기존 `code.gs` + 구글시트 **100% 그대로 유지**, 배포 URL만 프론트 코드에 연동 |
| 동시 접속 목표 | 학급 단위 약 30명 |
| 개발 도구 | Google Antigravity (에이전트형 IDE) |
| 시각 레퍼런스 | 업로드 이미지 2종 — 파스텔톤 픽셀아트 팜코지(Farm-cozy) 스타일 탑다운 마을 |

### 1.1 무엇이 바뀌고, 무엇이 그대로인가

| 구분 | 유지 (절대 변경 금지) | 변경 (신규 개발) |
|---|---|---|
| 데이터 스키마 | 시트 10개, 컬럼 구조, `SCHEMA` 상수 | — |
| 비즈니스 로직 | `dispatch()` 내부 약 90개 함수(주식 매매, 예금 이자, 마트 결제, 급여 계산 등) | — |
| 인증 | 이름+비밀번호 로그인(`studentLogin`), 관리자 비밀번호(`adminAuth`) | 로그인 흐름의 진입 UI만 게임 로그인 화면으로 재구성 |
| 화면 | 탭/서브탭 리스트 UI | **2D 맵 + 건물 + 캐릭터 이동 + 실시간 채팅** |
| 호스팅 | — | GAS HtmlService → **GitHub Pages**로 프론트엔드 이전 |
| 통신 방식 | — | `google.script.run` → **`fetch()` 기반 REST 통신**으로 전환 (필수, 아래 2.1 참조) |
| 실시간성 | — | **Firebase Realtime Database 신규 도입** (캐릭터 좌표/채팅 전용) |

---

## 2. 전체 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│  GitHub Pages (정적 호스팅, 프론트엔드 전체)                │
│  ─ Phaser 3 게임 클라이언트 (index.html + js/)             │
│  ─ 화면: 로그인 → 2D 월드(TownScene) → 건물 진입 모달         │
└───────────────┬───────────────────────┬───────────────────┘
                │ fetch(POST, JSON)      │ Realtime listener
                ▼                        ▼
┌───────────────────────────┐  ┌───────────────────────────┐
│  기존 code.gs (Apps Script) │  │  Firebase Realtime DB(신규) │
│  Web App(/exec)로 배포      │  │  players/ , chat/ , presence/│
│  dispatch(action,payload)  │  │  ─ 캐릭터 좌표, 방향, 애니메이션│
│  ─ 자산/거래/마트/부동산/    │  │  ─ 실시간 채팅 메시지          │
│    숙제/공지 등 모든 "경제·  │  │  ─ 접속자 목록(presence)      │
│    행정" 데이터 담당          │  │                            │
└───────────────┬───────────┘  └───────────────────────────┘
                ▼
        Google Sheets (10개 시트, 기존 DB 그대로)
```

**데이터 책임 분리 원칙**

| 데이터 | 저장 위치 | 이유 |
|---|---|---|
| 현금/주식/예금/재고/성적/출결성 데이터 | 구글시트(기존) | 영속성·감사(audit)·교사 열람이 중요 → 변경 불가 |
| 캐릭터 실시간 좌표·방향·애니메이션 상태 | Firebase RTDB | 초당 여러 번 갱신되는 휘발성 데이터, Sheets에 넣으면 안 됨 |
| 실시간 채팅 메시지 | Firebase RTDB (+ 선택적으로 GAS를 통해 시트에 비동기 백업) | 즉시성 필요 + 학교 특성상 사후 열람용 로그도 필요 |
| 접속자 목록(누가 지금 접속 중인지) | Firebase RTDB `presence` | `onDisconnect()` 같은 실시간 이탈 감지가 시트로는 불가능 |

### 2.1 핵심 설계 결정: 왜 GAS 단독으로는 안 되는가 ⚠️

이 프로젝트에서 가장 중요한 기술적 결정이므로 Antigravity 작업 전에 반드시 이해하고 넘어가야 합니다.

1. **`google.script.run`은 GitHub Pages에서 쓸 수 없습니다.** 이 API는 HTML이 Apps Script의 HtmlService 컨테이너(iframe) 안에서 서빙될 때만 동작합니다. 프론트엔드를 외부 도메인(GitHub Pages)으로 옮기는 순간, 통신 방식은 **`fetch()`로 배포된 Web App URL(`/exec`)을 호출하는 REST 방식**으로 반드시 바뀌어야 합니다. `dispatch()` 자체는 그대로 두고, 이를 감싸는 `doGet`/`doPost` 진입점만 JSON API 형태로 바꾸면 됩니다.
2. **CORS/Preflight 이슈.** Apps Script Web App은 커스텀 CORS 헤더 설정이 자유롭지 않아, `Content-Type: application/json`으로 `fetch` POST를 보내면 브라우저가 OPTIONS 프리플라이트를 먼저 보내고 이 요청이 차단되는 경우가 흔합니다. 실무적으로 **`Content-Type: text/plain;charset=utf-8`로 보내 프리플라이트를 회피하고, 서버(`doPost`)에서 `e.postData.contents`를 직접 `JSON.parse()`** 하는 방식이 안정적입니다.
3. **실시간 이동 동기화를 GAS/Sheets로 처리하면 안 됩니다.** 30명이 각자 100~200ms 간격으로 좌표를 갱신한다고 가정하면 분당 수천 건의 호출이 발생합니다. Apps Script는 "동일 계정에서 너무 많은 스크립트가 동시 실행 중"이라는 동시 실행 제한 오류를 발생시키도록 설계되어 있고, Sheets 쓰기는 한 번에 200ms~1초 가까이 걸릴 수 있어 이 트래픽을 감당하지 못합니다. → **좌표/채팅처럼 "휘발성 + 고빈도" 데이터는 Firebase RTDB로 분리하고, GAS는 "영속성 + 저빈도"인 경제/행정 데이터만 전담**하는 하이브리드 구조가 30명 동시접속을 실제로 버틸 수 있는 유일한 현실적 방법입니다.
4. **Firebase 도입 트레이드오프**를 인지하고 진행해야 합니다.

| 장점 | 단점/주의점 |
|---|---|
| 실시간 리스너 내장, 좌표/채팅 동기화에 최적화 | 신규 인프라 하나가 추가됨(관리 포인트 증가) |
| 무료(Spark) 티어로 학급 단위 트래픽 충분히 처리 가능 | 무료 티어 동시 연결 수 상한이 있어 여러 학급이 같은 프로젝트를 동시에 쓰면 학기말 등 트래픽 급증 시 모니터링 필요 |
| `onDisconnect()`로 퇴장 처리가 깔끔함 | 별도 Firebase 프로젝트 생성/보안 규칙 설정 학습 필요(단, 사용자는 Firebase 사용 이력 있음) |

---

## 3. 데이터베이스 구조 — 변경 없이 그대로 사용

기존 `code.gs`의 `SCHEMA` 상수에 정의된 시트 10개를 **한 글자도 바꾸지 않고** 그대로 사용합니다. 아래는 Antigravity가 데이터 흐름을 이해할 수 있도록 정리한 참고표입니다.

| 시트명 | 용도 | 비고 |
|---|---|---|
| 학생 | 학생 마스터(이름/비밀번호/직업/현금/주식/상태/레벨/권한) | "권한" 컬럼 값으로 시청(위임) 접근 여부 판단 |
| 거래LOG | 현금거래·세금·주식주문·고용·구직·부동산 전체 이력 | |
| 자산현황 | 예금·인벤토리·중고거래·상점 아이템 | |
| 주식시장 | 주가 이력 + 주식 뉴스 | |
| 학급활동 | 자기평가 체크리스트, 감정신호등, 호출(교사 부르기) | |
| 학습관리 | 숙제, 공지사항, 시간표 | |
| 자리배치 | 좌석(부동산) 배치도 + 거래 이력 | "부동산 중개소" 건물의 데이터 소스 |
| 설정 | 급여/세금/이자율 등 전역 설정값 | |
| 복권 | 복권 종류/확률/당첨금 | |
| 과제 | 과제 등록/제출/파일 업로드 | |

> **🔒 고정 요구사항**: `SPREADSHEET_ID`, `SCHEMA`, `dispatch()` 내부 함수 시그니처는 변경하지 않습니다. 새로 필요한 게임 전용 데이터(예: 캐릭터 스킨 선택값 등 영속 저장이 필요한 항목)가 생기면 기존 "학생" 시트에 컬럼을 추가하거나 새 시트를 만드는 방식으로 확장하고, 기존 컬럼은 건드리지 않습니다.

---

## 4. 기능 → 건물 매핑 (기능분석)

기존 하단 네비게이션(`홈·LMS·경제·자기평가·권한·설정`)과 서브탭들을 아래와 같이 **14개 건물 + 1개 상시 UI**로 재배치합니다.

| 기존 위치 | 신규 건물 | 한글 간판 | 배치 구역 | 진입 방식 | 대표 dispatch action |
|---|---|---|---|---|---|
| 홈 › 내 자산 · 랭킹 | 내 집(플레이어 하우스) | "◯◯◯의 집" (이름 동적 표시) | 주거구역 | 모달 | `getInitData`, `getRankingData` |
| 경제 › 예금/상점(예금) | 은행 | "은행" | 금융구역 | 모달 | `getDeposits`, `depositMoney`, `withdrawDeposit` |
| 경제 › 예금/상점(상점) | 잡화점 | "잡화점" | 상업구역 | 모달 | `getShopItems`, `buyItem`, `getInventory` |
| 경제 › 주식 | 증권거래소 | "증권거래소" | 금융구역 | 모달 | `getStockData`, `tradeStock` |
| 경제 › 부동산 | 부동산 중개소 | "부동산 중개소" | 금융구역 | 모달 | `getRealEstateData`, `buySeat`, `sellSeat` |
| 경제 › 장터 | 벼룩시장 | "벼룩시장" (야외 좌판 형태 권장) | 공원구역 | 근접 시 바로 모달(건물 없이 노점) | `getMarketItems`, `buyMarketItem` |
| 경제 › 복권 | 복권방 | "행운의 복권방" | 상업구역 | 모달 | `getLotteryInfo`, `buyLottery`, `scratchLottery` |
| 경제 › 구직 | 고용센터 | "고용센터" | 행정구역 | 모달 | `getJobMarket`, `applyJob` |
| 경제 › 송금 | 우체국 | "우체국" | 행정구역 | 모달 | `transferMoney` |
| 경제 › 마트 | 학급마트 | "학급마트" | 상업구역 | 모달 | `getMartItems`, `martPay` |
| LMS 전체(호출·숙제·과제·급식·공지) | 학교(본관) | "학교" | 교육구역 | 씬 내부 다중 탭 모달 | `sendCall`, `getHomework`, `getAssignments`, `getMeal`, `getNotices` |
| 자기평가·감정신호등 | 상담실 | "상담실" | 교육구역(학교 부속) | 모달 | `saveChecklist`, `logEmotion` |
| 권한(위임) | 시청 | "시청" | 행정구역 | 모달, **"권한" 없는 학생은 문이 잠긴 연출** | `delegatedPay`, `delegatedFine`, `delegatedPraiseCard`, `delegatedWarning` |
| 관리자(교사) 전용 | 교장실 | (간판 없음 · 히든) | 학교 부속, 별도 진입 트리거 | 비밀번호 입력 모달 → 전체 관리자 패널 | `adminAuth`, `adminGetAllData` 등 전체 |
| 설정 | — | — | 건물 아님, 캐릭터 옆 상시 톱니바퀴 아이콘 | 즉시 모달 | `changePassword` |

---

## 5. 맵 & 월드 디자인 🎨

### 5.1 비주얼 컨셉

업로드된 참조 이미지는 파스텔톤의 탑다운 팜코지(Farm-cozy) 스타일입니다 — 32px 단위 타일, 완만한 그림자, 지붕 색으로 건물을 구분(파랑/빨강/초록), 집 앞에 우편함·꽃밭·표지판 배치, 흙길과 잔디의 경계가 부드러운 픽셀아트. 이 톤을 그대로 유지하는 것을 권장합니다.

### 5.2 구역(Zone) 설계

| 구역 | 배치 건물/요소 | 컨셉 |
|---|---|---|
| 중앙광장(스폰 지점) | 분수대, 게시판(공지 미리보기), 안내 표지판 | 모든 학생이 로그인 시 스폰되는 곳 |
| 금융구역 | 은행, 증권거래소, 부동산 중개소 | 단정한 벽돌/석재 건물 톤 |
| 상업구역 | 잡화점, 학급마트, 복권방 | 알록달록한 천막/파라솔 장식 |
| 행정구역 | 시청, 고용센터, 우체국 | 깃발/우체통 등 관공서 소품 |
| 교육구역 | 학교(본관), 상담실, 교장실 | 운동장·화단이 딸린 넓은 부지 |
| 주거구역 | 학생 수만큼의 "내 집" 또는 기숙사형 단일 건물 | 아래 5.3 참고 |
| 공원/호수구역 | 벤치, 호수, 다리, 벼룩시장 좌판, 가로등 | 순수 사교·휴식 공간, 채팅이 가장 활발할 곳으로 설계 |

### 5.3 "내 집" 처리 방식 (설계 제안)

30명 각각의 집을 맵에 물리적으로 배치하면 맵이 지나치게 비대해집니다. 두 가지 대안 중 선택을 권장합니다.

- **안 A (권장)**: 주거구역에 "기숙사" 건물 1채를 두고, 입장 시 접속한 본인의 개인 대시보드(자산/랭킹/인벤토리)가 뜨는 방식. 맵 크기와 개발 난이도가 합리적입니다.
- **안 B**: 참조 이미지처럼 소수(4~6채)의 대표 주택만 배치하고 장식용 NPC 하우스로 두되, 클릭 시 무작위/자산순위 상위 학생 정보를 보여주는 전시용으로 활용. 순수 장식 목적이라면 이 방식도 가능합니다.

### 5.4 맵 규모 권장치

동시 30명이 겹치지 않고 자연스럽게 흩어질 공간이 필요합니다. 타일 32px 기준 **가로 100~120 × 세로 80~100 타일** 정도(7개 구역 + 이동 동선 확보)를 시작점으로 제안하며, Phaser 카메라는 캐릭터를 따라가는 줌 1.5~2배 정도의 근접 시점을 권장합니다.

### 5.5 장식 요소 체크리스트

호수(다리 포함), 벤치, 공원 화단, 가로등, 우편함(건물마다 참조 이미지처럼 1개씩), 나무·그루터기·바위(충돌 오브젝트 겸용), 게시판, 분수대, 계절 이벤트를 고려한 교체 가능한 데코 레이어.

### 5.6 에셋 소싱 가이드 ⚠️

특정 상업 게임의 타일셋을 그대로 추출해 쓰는 것은 저작권 문제가 있으므로 금지합니다. 대신:
- **Kenney.nl** — CC0(퍼블릭 도메인) 게임 에셋을 다수 제공, 상업적 이용도 자유로워 학교 웹앱에 가장 안전합니다.
- **itch.io**의 "무료(Free)" 필터 + 라이선스가 CC0 또는 학교/교육용 사용을 명시적으로 허용하는 팩을 다운로드해 사용하고, 반드시 각 팩의 라이선스 조건(출처 표기 필요 여부 등)을 README에 기록해 둘 것을 권장합니다.

---

## 6. 클라이언트 기술 스택 & 씬 구조

**엔진: Phaser 3** — 2D 타일맵, 충돌, 카메라, 스프라이트 애니메이션을 표준 지원하고 GitHub Pages 같은 순수 정적 호스팅에 그대로 배포 가능해 이 프로젝트의 제약과 가장 잘 맞습니다. 맵 데이터는 **Tiled**(무료 맵 에디터)로 JSON export하여 사용을 권장합니다.

| 씬(Scene) | 역할 |
|---|---|
| `BootScene` | 최소 로딩 리소스만 불러와 프리로더 표시 |
| `PreloadScene` | 타일셋/캐릭터 스프라이트시트/UI 이미지 전체 로드 |
| `LoginScene` | 기존 `studentLogin` 연동 로그인 화면 (2D 마을 배경 위 로그인 카드) |
| `TownScene` | 메인 월드 — 타일맵, 충돌 레이어, 건물 오브젝트, 다른 플레이어 스프라이트 |
| `UIOverlayScene` | TownScene 위에 항상 떠 있는 채팅창·미니맵·현금 HUD (Phaser의 병렬 씬으로 구현) |
| `BuildingModalScene` (또는 DOM 오버레이) | 건물 진입 시 뜨는 기능별 UI. 기존 index.html의 화면 로직을 재사용하되 통신부만 fetch로 교체 |

모바일 대응: 화면 좌하단 가상 조이스틱 + 우하단 상호작용 버튼("E"에 대응)을 터치 UI로 제공합니다.

---

## 7. 실시간 멀티플레이어 & 채팅 (Firebase RTDB)

### 7.1 데이터 스키마 (제안)

```
players/
  {studentName}/
    x: 1024
    y: 768
    dir: "down"          // up/down/left/right
    moving: true
    lastSeen: 1735000000

chat/
  general/
    {pushId}/
      name: "홍길동"
      msg: "은행 어디예요"
      ts: 1735000000

presence/
  {studentName}: true    // onDisconnect()로 자동 false/삭제
```

### 7.2 동기화 정책

- 좌표 갱신은 **100~150ms 쓰로틀**로 전송, 수신 측은 보간(interpolation)으로 부드럽게 렌더링.
- 로그인(`studentLogin` 성공) 시점에만 `presence`를 `true`로 세팅하고, `onDisconnect().remove()`로 창을 닫거나 네트워크가 끊겨도 자동으로 접속자 목록에서 제거되도록 구성.
- 채팅은 전체 채팅 1개 채널로 시작하고, 필요시 구역별(공원/학교 등) 채널 분리는 2차 확장으로 제안.

### 7.3 학교 환경 특화 고려사항

- 간단한 금칙어 필터(클라이언트 1차 필터 + 서버리스 함수 또는 GAS를 통한 2차 필터는 선택)를 적용합니다.
- 채팅 로그를 사후에 교사가 열람할 수 있도록, Firebase에 쌓인 메시지를 주기적으로(또는 즉시) GAS `dispatch`를 통해 구글시트에 백업 저장하는 옵션을 권장합니다(생활지도 목적). 이 백업 함수는 새 시트(예: "채팅LOG")를 하나 추가하는 것으로 기존 스키마를 해치지 않고 구현 가능합니다.

### 7.4 인증 연동

Firebase는 **익명 인증(Anonymous Auth)**만 사용하고, 로그인 성공 직후 기존 `studentLogin`이 반환한 학생 이름을 Firebase 세션의 표시 이름으로 매핑합니다. 즉, "진짜 인증"은 여전히 구글시트 기반 `studentLogin`이 담당하고, Firebase 인증은 실시간 연결을 위한 최소한의 세션 식별용으로만 사용합니다.

---

## 8. 건물 인터랙션 UX 플로우

1. 캐릭터가 건물 반경 안에 들어오면 건물 위에 말풍선/아이콘으로 진입 안내 표시
2. PC: `E` 키 또는 스페이스바 / 모바일: 상호작용 버튼 탭
3. 해당 건물에 매핑된 기능 UI가 모달(또는 DOM 오버레이)로 열림 — 이 내부 UI는 기존 `index.html`의 화면·로직을 최대한 재사용하고 통신부만 `google.script.run` → `fetch`로 교체
4. 시청처럼 "권한" 컬럼 값이 없는 학생이 접근하면 잠긴 문 연출 + "권한이 필요합니다" 안내
5. 모달을 닫으면 캐릭터는 건물 진입 전 위치에 그대로 유지

---

## 9. 인증 및 권한 체계

| 역할 | 판별 기준(기존 데이터 그대로 사용) | 접근 가능 건물 |
|---|---|---|
| 일반 학생 | `studentLogin` 성공 | 내 집, 은행, 잡화점, 증권거래소, 부동산, 벼룩시장, 복권방, 고용센터, 우체국, 학급마트, 학교, 상담실 |
| 권한 위임 학생(반장 등) | "학생" 시트의 "권한" 컬럼 값 보유 | 위 전체 + 시청 |
| 교사(관리자) | `adminAuth(pw)` 통과 | 교장실(전체 관리자 패널) |

> **🔒 보안 체크리스트**: `SPREADSHEET_ID`, `ADMIN_PASSWORD`, `NEIS_KEY` 등 `code.gs`에 있는 상수는 절대 GitHub 저장소(클라이언트 코드)에 옮기지 않습니다. GitHub Pages에 노출되는 것은 오직 **배포된 Web App URL(`/exec`)** 뿐이어야 하며, 이는 기존에도 공개 접근을 전제로 설계된 값이므로 문제되지 않습니다. 교장실(관리자) 진입 트리거는 맵 위에 간판을 달지 않는 등 눈에 잘 띄지 않게 배치해 일반 학생의 우연한 진입 시도를 줄이는 것을 권장합니다.

---

## 10. 리포지토리 구조 & API 연동 스펙

### 10.1 폴더 구조 (제안)

```
classbank-town/
├─ index.html
├─ config.js              # GAS_URL, FIREBASE_CONFIG 상수만 보관
├─ js/
│  ├─ scenes/              # BootScene, TownScene 등
│  ├─ api.js               # GAS fetch 브릿지
│  ├─ realtime.js          # Firebase 좌표/채팅 연동
│  └─ ui/                  # 건물별 모달 UI (기존 index.html 로직 이식)
├─ assets/
│  ├─ tilesets/
│  ├─ characters/
│  └─ ui/
└─ maps/
   └─ town.json            # Tiled로 제작한 타일맵
```

### 10.2 클라이언트 API 브릿지 (예시 시그니처)

```js
// js/api.js
async function callAPI(action, payload = {}) {
  const res = await fetch(GAS_URL, {
    method: 'POST',
    headers: { 'Content-Type': 'text/plain;charset=utf-8' }, // CORS 프리플라이트 회피
    body: JSON.stringify({ action, payload })
  });
  return res.json();
}
// 사용 예: const data = await callAPI('getInitData', { name });
```

### 10.3 `code.gs` 측 최소 변경사항

기존 `dispatch()`와 90여 개 함수는 **그대로 두고**, 진입점(`doGet`/`doPost`)만 JSON API 형태로 바꿉니다.

```js
function doPost(e) {
  const body = JSON.parse(e.postData.contents);
  const result = dispatch(body.action, body.payload);
  return ContentService.createTextOutput(JSON.stringify(result))
    .setMimeType(ContentService.MimeType.JSON);
}

// 기존 doGet()은 더 이상 index.html을 서빙할 필요가 없으므로
// 상태 확인용(healthcheck)으로 단순화하거나, 필요시 GET 파라미터로도
// 동일 dispatch를 호출하도록 확장 가능(단, 민감 데이터는 POST 사용 권장)
```

이후 Apps Script 편집기에서 **"새 배포"**로 재배포해야 새 `doPost` 로직이 반영된 `/exec` URL이 발급됩니다.

---

## 11. 개발 로드맵

| Phase | 목표 | 주요 산출물 | 완료 기준 |
|---|---|---|---|
| 0. 사전 준비 | 에셋 수급, Firebase 프로젝트 생성, GAS 배포 설정 전환 | 라이선스 정리된 에셋 폴더, Firebase 프로젝트, 새 `/exec` URL | doPost 응답 확인(healthcheck) |
| 1. 정적 월드 프로토타입 | 로그인 없이 로컬에서 맵 이동만 구현 | TownScene, 충돌 타일맵 | 캐릭터가 벽/건물에 막히며 자유 이동 |
| 2. API 브릿지 연동 | fetch 기반 로그인·자산조회 성공 | LoginScene ↔ `studentLogin` 연동 | 로그인 성공 시 실제 시트 데이터 표시 |
| 3. 건물별 기능 이식 | 14개 건물 UI를 기존 기능과 1:1 매핑 | 건물별 모달, 기능 커버리지 체크리스트 | 4장의 매핑표 전 항목 동작 확인 |
| 4. 실시간 레이어 | Firebase 좌표 동기화 + 채팅 | realtime.js, UIOverlayScene 채팅창 | 2개 기기에서 서로의 캐릭터/채팅 실시간 확인 |
| 5. 부하 테스트 & 최적화 | 30명 동접 시뮬레이션 | 스로틀링/보간 튜닝 결과 | 30명 동시 접속 시 프레임/지연 허용 범위 내 |
| 6. 배포 & 운영 가이드 | GitHub Pages 정식 배포, 교사용 안내서 | 배포 URL, 운영 매뉴얼 | 실제 학급에서 시범 운영 |

---

## 12. 배포 방법

1. **GAS**: Apps Script 편집기 → 배포 → 새 배포 → 유형 "웹 앱" → 실행 권한 "나(본인)", 액세스 권한 "모든 사용자(익명 포함)" → 배포 후 발급되는 `/exec` URL을 `config.js`의 `GAS_URL`에 입력. (기존 `doGet`을 수정했으므로 반드시 **새 배포**로 갱신 — "배포 관리"에서 기존 배포를 수정하면 URL이 안 바뀌므로 편리)
2. **GitHub Pages**: 저장소 생성 → 코드 push → 저장소 Settings → Pages → 소스 브랜치 지정(예: `main`, `/root`) → 발급된 `https://{계정}.github.io/{저장소}/` 확인
3. **Firebase**: Firebase 콘솔에서 프로젝트 생성 → Realtime Database 활성화(테스트 모드로 시작 후 아래 보안 규칙 적용) → 웹 앱 등록 → 발급된 config 객체를 `config.js`의 `FIREBASE_CONFIG`에 입력

**Firebase 보안 규칙(예시, 무분별한 외부 쓰기 방지)**
```json
{
  "rules": {
    "players": { ".read": true, ".write": "auth != null" },
    "chat":    { ".read": true, ".write": "auth != null" },
    "presence":{ ".read": true, ".write": "auth != null" }
  }
}
```

---

## 13. 리스크 매트릭스

| 리스크 | 발생 가능성 | 영향도 | 대응 방안 |
|---|---|---|---|
| GAS 동시 실행 제한 오류 | 중 | 높음 | 좌표/채팅은 반드시 Firebase로 분리, GAS 호출은 "경제 행위"(구매/이체 등) 저빈도 액션에만 한정 |
| Sheets 응답 지연으로 인한 체감 렉 | 중 | 중 | 건물 진입 시 로딩 스피너 + 낙관적 UI(요청 성공을 가정하고 먼저 화면 갱신 후 실패 시 롤백) 적용 |
| CORS/프리플라이트 실패 | 높음(초기 구현 단계) | 중 | `text/plain` 트릭 적용, 실패 시 에러 메시지를 사용자에게 명확히 표시 |
| Firebase 무료 티어 초과 | 낮음 | 낮음 | 학급 1개 단위 트래픽은 Spark 무료 플랜으로 충분, 여러 학급 동시 운영 시에만 모니터링 |
| 에셋 저작권 문제 | 중 | 높음(배포 후 법적 이슈) | CC0/명시적 교육용 허용 라이선스 에셋만 사용, 출처 기록 |
| 관리자 진입 경로 노출 | 낮음 | 높음(성적/자산 데이터 접근) | 교장실 간판 비노출, `ADMIN_PASSWORD` 클라이언트 코드 미포함 |

---

## 14. Antigravity 실행 요약 (한 페이지 요약)

> Phaser 3 기반 2D 탑다운 마을 게임을 GitHub Pages에 배포한다. 기존 `code.gs`(첨부)의 `dispatch()`와 90여 개 함수, 구글시트 10개 스키마는 절대 변경하지 않는다. `doGet`/`doPost`만 JSON API 형태로 감싸고, 프론트엔드는 `fetch(GAS_URL)`로 통신한다(단, `text/plain` Content-Type으로 CORS 프리플라이트 회피). 기존 탭 UI(마트/은행/증권/부동산/장터/복권/구직/송금/LMS/자기평가/권한/관리자)를 4장의 매핑표대로 14개 건물로 재배치하고, 각 건물에 한글 간판을 단다. 참조 이미지 스타일(파스텔톤 팜코지 픽셀아트)로 5장의 구역 설계에 따라 은행·상업·행정·교육·주거 구역과 공원·호수를 배치한 넓은 맵(약 100×80 타일)을 만든다. 캐릭터 이동/애니메이션/충돌은 Phaser로 처리하고, 다른 플레이어의 실시간 좌표와 채팅은 신규 도입하는 Firebase Realtime Database(스키마는 7.1 참조)로 동기화해 최대 30명 동시접속을 지원한다. 로그인은 기존 `studentLogin`을 그대로 쓰고, "권한" 컬럼 보유자만 시청, `adminAuth` 통과자만 교장실에 입장 가능하도록 한다. 개발은 11장의 Phase 0~6 순서로 진행한다.
